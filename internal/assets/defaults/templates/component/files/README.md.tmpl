# {{ name | pascal }}

Owned by {{ owner }}.

TODO: document the {{ name }} component. Put its sources next to this
file; `arclint make components/{{ name | kebab }}` keeps it in sync with
the template.
