# {{ name | pascal }}

TODO: describe {{ name }}.

## Development

CI runs on every push to {{ default_branch }} (see .github/workflows/ci.yml).
Architecture conventions are linted with `arclint check`.
