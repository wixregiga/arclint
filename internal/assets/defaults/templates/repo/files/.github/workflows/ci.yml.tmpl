name: ci

on:
  push:
    branches: [{{ default_branch }}]
  pull_request: {}

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: architecture lint
        run: echo "add build, test, and 'arclint check' steps here"
