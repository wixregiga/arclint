# Normalize every text file to LF in blobs and working trees alike, so
# checkouts made by Windows-side git (autocrlf=true) cannot break the
# byte-exact drift and fixture tests. `text=auto` leaves files git
# detects as binary untouched; the repo currently tracks no binaries.
* text=auto eol=lf
